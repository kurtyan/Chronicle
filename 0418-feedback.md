服务端修改
- 打包后的应用能否安装到本地的npm repository？
- 每次用脚本启动还要加nohup，很麻烦。能否做成mac下的系统服务？
- 我需要有日志输出，需要能在config.json中配置日志文件路径


客户端修改  
- 背景颜色太白了，需要加点黄，需要有质感一点  
- 客户端颜色要跟随系统颜色
- 问题：创建标题时中文输入法里输入英文后按回车时，应当是输入英文，但目前标题框直接捕获了enter键，并把光标跳转到了正文
- 创建标题时，按tab，跳转到了富文本编辑框的工具栏。需要改成：在标题栏按tab，光标跳转到富文本正文
- 点击已有处理记录中的链接时，也进入了编辑模式。需要改成：点击链接后，打开链接
- 点击链接，目前默认从tauri里打开了。要改成从系统默认浏览器打开
- 开始编辑处理记录时，会触发自动take over，但自动take over没有自动把任务置为开始
- 按cmd + -，需要能缩放
- 需要一个logo，请根据Chronolic这个concept帮我生成一个Logo
- 每个任务创建后，自动生成一个字符串ID，格式：  T0000000  ，放置在标题这一行的最右侧。同时加一个复制按钮，单击后自动复制任务ID
- 工作报告页的修改：
  - 标题 太紧贴左边了，需要有padding
  - 需要把 Day Week Month 日期切换空间挪到整个Report页的最上方
  - Today Total, Completed, In Progress, 这三个需要根据上方选择的日期或者时间段变化。

客户端-服务端协议修改（探讨）：
- 我计划通过claude集成服务端api来操作任务的take over、afk、done，所以需要claude能接入api
- 当通过api修改了某个任务时，我希望客户端能得到推送并实时刷新
- 请根据以上场景分析协议是否要修改为长连接和双向推送，或者引入客户端刷新机制